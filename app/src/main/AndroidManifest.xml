<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whitdan.arkhamhorrorlcgcampaignguide">

    <application
        android:name=".GlobalVariables"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".selectcampaign.SelectCampaignActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".campaignsetup.CampaignSetupActivity" />
        <activity android:name=".scenariosetup.ScenarioSetupActivity" />
        <activity android:name=".finishscenario.FinishScenarioActivity" />
    </application>

</manifest>